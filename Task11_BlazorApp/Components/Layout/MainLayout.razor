@inherits LayoutComponentBase

<MudThemeProvider />

<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar>
       <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="DrawerToggle"/>
       <MudText Typo="Typo.h4">Finance App</MudText>
        <MudIconButton Icon="@Icons.Material.Filled.AttachMoney" Color="Color.Inherit"></MudIconButton>
    </MudAppBar>
    <MudDrawer @bind-Open="@drawerOpen">
        <NavMenu/>
    </MudDrawer>
    <MudMainContent>
        @Body
    </MudMainContent>
</MudLayout>

@code{
    bool drawerOpen = false;

    void DrawerToggle()
    {
        drawerOpen = !drawerOpen;    
    }
}